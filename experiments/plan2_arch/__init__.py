"""Plan 2 — architecture modifications for EfficientNetB0, isolated package.

Each tier is a standalone row against the fixed `efficientnetb0_on` baseline,
changing exactly one architectural thing. Tiers are never bundled with each
other or with Plan 1's background randomization.
"""
